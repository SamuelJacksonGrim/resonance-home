# Node
node_modules/
npm-debug.log*
*.log

# Local house config + secrets (the real one is per-machine; the .example is tracked)
home-config.json
.env
.env.*

# Build intermediates + single-file exe (added in RH-12; regenerated, never tracked)
build/
dist/
*.blob
*.exe
memory
resonance-home
embedded-assets.js

# Local backups + OS cruft
*.bak
*.bak-*
.DS_Store
Thumbs.db
